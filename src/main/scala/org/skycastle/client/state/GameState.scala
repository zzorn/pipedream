package org.skycastle.client.state

/**
 * Keeps track of the state of the perceived game world, both the outer one (terrain, other entities, chats, etc),
 * as well as the inner (inventory, attributes, etc).
 */
trait GameState {

}